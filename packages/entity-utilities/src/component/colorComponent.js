/**
 * Manages the render color of an entity.
 *
 * @param {number} color The entity's color.
 * @returns {{ color: number }} The new color component.
 */
export function colorComponent(color) {
	return { color }
}
