// Module imports
import { useTick } from '@pixi/react'





/**
 * Uses the Pixi.js ticker to run all systems in the game loop.
 */
export function useGameLoop() {
	useTick(() => {
		// Execute scale functions
	})
}
