/**
 * Detects collisions and updates entities accordingly.
 *
 * @param {object} store Statery store with the game's state.
 */
export function physicsSystem(store) {
	console.log('physicsSystem', store)
}
